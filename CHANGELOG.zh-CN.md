# 更新日志

[English](CHANGELOG.md)

PhD Atlas 公开版的所有重要变化都记录在此文件中。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [0.1.1] - 2026-08-17

### 变更

- 源码和公开分发恢复使用 MIT License；之前的 PhD Atlas Community License v1.0
  不再是当前许可证。

## [0.1.0] - 2026-08-10

这是 PhD Atlas 第一个公开稳定版。以下内容完整记录从 `0.1.0-beta.8` 到本版本的
变化；除了看得见的新功能，也保留了为了让“稳定版”三个字名副其实而完成的可靠性工作。

### 新增

- 每个申请中新增一套连续的 Admissions 证据工作台。录取结果、不同申请周期的结果
  分布、导师经费、项目、论文、未匹配证据、收藏与最终来源清单各自分开，公开论坛的
  个人分享不会再被包装成学校官方录取率。
- 新增无需 OAuth 的 Reddit Atom 历史申请数据来源。多个搜索变体独立成败，成功帖子
  保留帖子级永久链接；学校或项目不匹配的内容只会留在“未匹配证据”中供核查，绝不进入
  录取统计。
- 在 NSF、NIH 与 OpenAlex 之外新增英国官方 UKRI Gateway to Research 项目证据。
  每条研究记录都保留精确来源网址、获取时间、匹配原因，以及“已核实/可能匹配”状态。
- 重新按当前官网核对 Princeton 神经科学项目与导师金标准，补齐 Graduate School、
  PNI、QCN、PACM、计算机系、个人主页和学校研究档案等正式入口。经过维护的学校适配器
  可以声明更大的有界页数预算，在某个官网主机间歇拒绝访问时仍会尝试完明确列出的备用
  入口；系统级 32 页绝对上限保持不变。导师金标准还可以记录官网使用的姓名变体，某个
  页面多出中间名首字母时，不会再把同一位导师误判成两个人或匹配失败。
- 新增与学校、项目和导师绑定的调研报告。目标发生变化后，旧报告会立即标记为过期并
  隐藏，直到新调研成功，避免把 A 申请的证据显示到 B 申请下面。
- 新增面向 Codex 与 Claude Desktop 的完整安装包：直接安装的 Skill ZIP、可选本地
  MCP Plugin ZIP，以及 Claude Desktop 一键 MCPB；每个包都有配套校验文件，并共用
  同一套账号权限边界。
- 新增浏览器设备授权流程。用户可以在批准前检查 scope，给连接改名、暂停、恢复或
  删除；授权有到期和长期未使用失效机制，账号安全版本变化或整库恢复后会立即失效。
- 新增用户自定义邮件分类、一封邮件多个标签、组合筛选，以及只会从允许分类中选择、
  可以返回多个标签的 AI 分类；AI 不能擅自创造新分类。
- 新增每把 AI 密钥独立的路由设置。密钥可以启用或暂停，分别设置权重和 1–2,500 的
  可见最大并发；OpenAI 兼容接口还可选择自动、Responses API 或 Chat Completions。
  加权调度仍受全局 2,500 安全上限约束。
- 新增覆盖 8 个真实项目、24 位导师的独立金标准与真实服务商验证总账。真实性和覆盖率
  分开判定，失败轮次不会被删除或包装成成功；模型可以提取证据，但不能自己证明自己。
- 时间线新增明确的事件类型：截止日期、提醒、任务、邮件、手动记录、奖学金、学费与
  申请费。每条事件都可以准确跳回产生它的原始项目。
- 上传到清单的文件现在明确提供“预览、下载、删除”三个操作，同时保留双击改名和
  只读模式保护。
- 申请表格新增页面底部批量操作栏、真实表头吸顶、接近底部时渐进加载，以及从表格
  切到看板后仍能保留有效选择的驻留状态。
- 新增逐字段的多语言校验和恢复提示。系统会明确指出、展开并聚焦真正有问题的字段，
  不再只扔出一句笼统的“校验失败”。
- 新增极限规模门禁：10 万条元数据、真实 2 万行加密数据库、300 个常驻 SSE、
  同一地址 300 个健康 WebSocket、重启后逐条读回、长时间自动保存与有界内存。
- 新增 `server/sources/` 外部招生数据适配层。每个数据源各自声明速率上限、缓存时长、
  并发数、超时、重试策略与 User-Agent，不再共用一套全局抓取行为；每条取到的数据都
  带上来源标识、精确 URL 与抓取时间，界面因此可以链回原页，数据过期时也看得出来。
  已接入：NSF Award Search 与 NIH RePORTER（官方接口、无需密钥，也是判断某位教授
  当前是否有经费招生的最强信号）、OpenAlex 论文、GradCafe 录取结果，以及走官方
  OAuth 接口或 Atom feed、而非抓取网页的 Reddit。
- 新增 `POST /api/interview-prep/ai/mock-turn`。面试后端本已能基于简历、导师主页与
  论文信号生成追问、评分与优劣势分析，但一直没有对外出口。模拟练习现在可以请求
  追问，返回的问题会追加到当前会话。
- 新增邮件动作：把一封邮件收进面试准备库，自动创建或恢复一个带有志愿、来源邮件、
  主题、学校、项目与导师信息的面试工作区。
- 新增 `GET /api/profile/recommenders` 游标分页接口与单条详情接口，推荐人目录不再
  必须整份下发。
- 新增耐久测试 `npm run qa:endurance`：多用户持续自动保存并逐条读回校验、单用户
  保存与后台任务并发、长时间 SSE 与 WebSocket 连接、以及进程存活监控。
- `/api/health` 新增事件循环延迟、常驻内存、内存预算与压力等级，线上状态可以直接
  读取，不必再从响应耗时反推。
- 新增邮件分类与 AI 输出质量的评测脚手架，配套带标注的样本，其中包含一封提示词
  注入攻击样本。

### 变更

- 公开分发许可证由 MIT 改为 PhD Atlas Community License v1.0。个人以及公益、
  慈善、非营利机构可用于个人、学术、科研、教育、公益和其他非商业目的；任何营利
  机构、商业组织或企业在使用前都必须取得书面授权，包括企业内部使用、SaaS、咨询、
  托管服务、转售，以及承包商代表企业使用。
- 公开版从 Beta 进入首个稳定版 `0.1.0`。今后最高稳定版更新 `stable` 和 `latest`
  容器通道，最高 Beta 只更新 `beta`，未来的新测试版不会把稳定用户拉回预发布通道。
- 当前运行、导航、升级、管理后台、默认测试与公开导出均已归档 Team 协作。实现与
  已存 Team 数据完整保留，供未来明确恢复；当前版本只提供个人版，不播种或加载 Team
  工作区。
- 申请进入 Dossier 改成只紧急读取当前记录。目标准备期间旧申请保持可见，连续快速
  点击只认最后一次意图，不再出现中间空白、旧 Inspector 或多余全列表读取。
- 看板进入与看板/表格切换改为可中断的驻留交接。点击立即有反馈，重树在过渡或空闲
  通道构建；从 Dossier 回到已打开的看板时直接复用，不再从零重建。
- Dossier 标签高亮走紧急路径，密集面板延后提交。新面板准备好之前保留旧面板，窄屏、
  触摸设备和减少动态效果模式都使用同一逻辑。
- 清单拖拽改为先确认真实顺序，再移除拖拽浮层。跨分组移动、被第二次拖拽打断、
  React 提交偏慢和减少动态效果都由同一个最终落点负责，不再弹回旧位置或二次跳动。
- 清单行更紧凑也更清楚：材料类型、分组和状态在桌面同一行；截止日期允许留空；
  完成勾选常驻；标题取消多余边框；改名时保留原扩展名；旧自定义材料仍可编辑删除。
- 申请时间线从“卡片套卡片”改成扁平语义行。标题、状态、来源、日期、说明、金额和
  编辑区按事件真正含义排版，在手机上也能自然换行。
- 通信页和编辑器进一步简化：数量放在标题旁，筛选与批量 AI 操作作为一整行进出，
  收发方向使用可访问的滑动控件，头像跟随真实作者，主题和日期使用安静的下划线。
- 申请批量操作移到页面级底部浮条，选择期间不再改变表格几何。看板与表格共用一条
  标题、帮助入口和控制基线。
- 申请切换、表格加载、批量选择、国家选择器、个人资料快捷入口、清单完成和语言切换
  统一使用有界的位移/透明度动效，并完整保留“减少动态效果”路径。
- 桌面端原生输入框、共享 Select、日期、费用、状态和相邻操作统一为同一套紧凑高度；
  手机端仍保留足够的触摸尺寸。
- 费用汇总与明细改用符合地区习惯的货币符号，例如加元显示 `CA$`；已支付、未支付、
  已豁免状态不再依赖装饰圆点，只保留清楚的文字和语义颜色。
- AI 邮件分类只处理收到的邮件。已发送邮件、草稿、笔记和手工记录的外发消息仍可
  普通整理，但前后端都会把它们排除在 AI 批量分类之外。
- 移除申请写入协议 v1 和其他已退休兜底路径。浏览器、API、离线队列、设置保存、
  工作区流、Codex CLI 和压力工具现在只使用同一套当前确认与变更协议。
- 工作区写入改为按租户分片。每个账号与团队各自持有版本号，两个人同时保存不再互相
  争抢；仅版本号冲突的情况由服务端重试并合并，不再抛给用户。审计事件已完全移出
  比较交换流程，改为幂等追加。
- libuv 线程池现在按主机 CPU 数在启动时设定。此前全仓库从未设置过，导致密码哈希、
  文件读写与压缩共用四条线程。密码准入同时受线程池大小与内存预算约束，排队发生在
  可观测、可限流的位置，而不是堆在没有超时也没有公平性的库内部。
- 超过 64 KiB 的数据在 worker 池中加密与序列化，worker 不可用时自动退回主线程。
- 默认内存预算改为 1 GiB，存在 cgroup 限制时优先采用。此前的 512 MiB 会把硬性上限
  压到正常工作区水合的峰值之下。
- 首屏改为请求列表形态的志愿数据，省略往来邮件正文、详情长文本、文件版本历史与
  评论正文。一条代表性记录从 386,221 字节降至 847 字节；选中某条时再完整加载。
  离线快照仍然存完整数据，在浏览器空闲时补齐。
- 推荐人目录改为服务端分页、worker 内聚合、分批渲染。1000 位推荐人与 200 个志愿的
  聚合现在约 50 毫秒完成。
- 导师检索能收集到更多候选。此前 OpenAlex 只读取第一页结果，而单个机构可能有数万名
  作者，候选池在任何上限生效之前就已经很小。现在改为有界游标分页，Crossref 在人数
  不足目标时即触发补召回，每所学校的学术证据从 36 位研究者提升到 48 位，每位研究者
  的近期论文从 3 篇提升到 5 篇，抓取并发从 3 提升到 6。另有四处上限经量化后确认不是
  瓶颈，刻意保持不变。
- Discover 现在把“证据穷尽”严格定义为：完整处理本轮实际抓取到的有限证据集，而不是
  声称抓遍整个互联网。面向用户的项目数和导师数配额已删除；每位导师必须先由个人官方
  主页确证，再补充学术数据，并用确定性的个人资料重合度排序，包括诚实保留零匹配结果。
- AI 补全申请的提取更完整。在真实服务商上实测，可用率从约五成提升到约九成。校验环节
  现在明确区分"证据中存在、必须提取"与"证据中不存在、应当留空"，把摘要类字段视为
  证据支持时的必填项，要求跨所有页面穷举申请要求而不是只看第一页，并且对已明确点名
  的导师保留姓名加注意事项，而不是直接清空。防编造约束一条未松，改造前后的编造计数
  始终为零。
- 动效经过审计并选择性修正。Discover 等级滑块拖动时与指针一比一跟随，指示器改用
  transform 而非布局属性做动画，命令面板打开不再有位移动效（它是每天使用多次的键盘
  入口），移动端弹层沿进入路径退出，提示条内容与槽位时序统一。长时间的装饰性动效
  经判断本来就是对的，保持原样。
- 触发限流的响应在所有路径上都携带 `Retry-After`，随附的 Nginx 模板新增针对登录鉴权
  的限流，返回结构化 JSON 429。
- 条件请求现已覆盖志愿列表、推荐人接口与 Discover 目录。

### 修复

- 修复把共享目录、学校行政结构、治理架构或普通说明页当成具体导师的问题。FAQ、简历、
  research staff、directory、organisational structure、university leadership 等栏目名
  不会再被当成人名；即使网页被来源声明为导师页，也必须在标题、正文或网址中找到真实
  姓名证据。学校使用纯数字个人主页、实验室或研究组共享页时，只要页面内容确实能对应
  到该导师，仍然可以正常保留。
- 修复 AI 服务已经返回响应头、读取正文时连接突然中断，进而让整批多项目 Discover
  调研直接失败的问题。连接、响应头、正文、DNS、超时和 socket 中断现在都会明确归类
  为“服务暂时不可用”，交给原有的有界重试恢复，而不是当成来历不明的错误终止整批任务。
- 修复临时 DNS 解析或公网 AI 服务连接失败被误报成“网址无效”的问题。现在这类错误会
  作为服务暂时不可用进入有界重试；格式错误、私网和保留地址仍然严格拒绝。
- 修复某些运行环境自带 `request.signal` 时，Interview Prep 正常请求会被错误取消并返回
  HTTP 499 的问题。现在只信任准入中间件明确创建的 AI 取消信号，读取完普通请求正文
  不会再把后续操作误判为用户取消。
- 修复云端公开发布门禁发现的两处跨环境所有权问题：有界流输出现在即使遇到平台分配器
  自动向上取整，也会拥有精确长度的底层缓冲；延迟 UI settle 回调会保留调度时的 Window，
  不再读取可能已被测试环境或宿主销毁的全局对象。
- 修复发布门禁连续验证私有源码和公开导出时，所有测试断言通过后仍可能因测试 worker
  退出而误报失败的问题。正式发布验收现在用单 worker 串行跑完整 Vitest 测试；日常开发
  测试仍保留更快的双 worker 默认值。
- 修复 HTTP 准入生命周期测试在 Linux 上的时序误报。服务端仍必须立即证明不完整请求
  和连接已经释放；测试会有界等待 Node 把异步 close 通知传到客户端对象，再核对客户端
  的镜像状态，不再假设两个运行时对象会在同一个事件循环瞬间同步。
- 修复本地开发或类生产重启时 API worker 已退出、Vite 却仍可访问的问题。持久监督器
  会有界重启异常 worker，但绝不会误杀碰巧占用 API 端口的未知进程。
- 修复一次网关或健康检查失败就把浏览器永久切进离线页的问题。短暂重启先按“恢复中”
  处理，持续主动探测并保留当前工作区，只有证据持续存在才进入安全离线模式。
- 修复存在未保存内容、远程登出或其他安全刷新因此被阻止时，申请搜索框里刚输入的内容
  仍可能在高负载下被清空的问题。搜索框现在会参加全局同步冲刷，较早到达的父层回声也
  不能再覆盖用户后来输入的新文字。
- 修复单人编辑也反复出现保存冲突的问题。旧浏览器与新服务端曾对邮件和分类字段的
  归属理解不同，导致同一申请永远算出不同指纹；现在在线、离线、差量和确认路径共用
  一份“用户字段/服务端字段”定义。
- 修复配额、备份、通知或日志写入导致大型工作区流被误判过期并重启的问题。最终校验
  只跟当前用户实际编辑的内容走，不再跟数据库全局杂项版本走。
- 修复自动备份每分钟重复扫过稳定记录的问题。每轮数量有上限，只处理从未备份或后来
  确实改过的申请，并优先补最旧缺口，不会让后面的申请永远排不到。
- 修复学校 Logo、外部招生来源等共享查询被多个界面同时触发后成倍发送相同请求的问题。
  进行中的请求、无结果、冷却时间和有界缓存现在可以安全复用。
- 修复大型加密工作区的内存尖峰。此前上一份响应还在压缩或写入网络，下一份就可能开始
  解码；现在大响应只有在传输真正结束后才释放下一份工作的名额。
- 修复 100 个正常账号同时打开工作区时，共享重任务队列把大量首次请求拒绝并逼成重试
  风暴的问题。新的按账号公平队列仍然有严格上限，但不会让一个账号挤掉其他账号。
- 修复重连和双击登录让相同密码哈希工作重复堆积的问题。现在只合并仍在进行中的同一项
  校验，不缓存最终认证结果，也不降低 Argon2、限流或准入要求。
- 修复删除申请越过正在保存的内容，或旧的整份 PUT 在删除后把申请“复活”的问题。
  保存、原子编辑和删除现在共用每个申请自己的顺序队列，回收站保留删除时的权威快照。
- 修复保留期扫描使用旧缓存后，误删刚被恢复记录的竞态。任何破坏性清理都会重新检查
  当前所有权和最新时间，不会让旧扫描结果直接决定删除。
- 修复 AI 草稿已经生成成功，却因为后续尽力而为的审计写入失败而向用户显示失败。
  成功结果会先交给用户，各类上游、格式、限流和体积错误也保留具体原因。
- 修复时间线图标被裁、Dossier 标题字母下沿被切、复选勾只画一半、折叠面板留下大片
  空白、版本行背景黏在一起，以及紧凑桌面宽度下工具栏控件消失等细节问题。
- 修复上传文件改名时如果用户没重新输入后缀就丢失扩展名；“分享上传”现在也会保留
  精确清单目标并自动带上上传权限。
- 修复引导草稿、通知发布和异步消息发送捕获旧回调的问题，较早完成的异步操作不再
  清空或覆盖用户后来输入的新内容。
- 修复分片写入路径中的死锁。重入判断此前依赖模块级的持有者变量，第二个租户获取车道
  时会将其覆盖，导致内层调用不再被识别为重入，转而去抢它自己外层已经持有的车道。
- 修复一处会终止服务器进程的崩溃。payload worker 超时回调调用了不在定时器作用域内的
  变量，而 Node 在定时器中遇到未捕获异常会直接终止进程，于是一次慢请求变成一次重启。
- 修复从未添加过推荐人的账号会永久停在加载页的问题。该设置缺失时每次渲染都会产生一个
  新数组，而聚合副作用依赖它，于是不断重跑、置状态、再重渲染，应用永远无法进入稳定
  绘制状态。
- 修复流式工作区引导缺少分页元数据的问题，否则流式客户端会拿到一份被静默截断到第一页
  且没有游标可继续的目录。
- 修复部署模板写死了会抵消运行时调优的取值：写死的线程池大小会让所有主机停留在公式
  下限，512 MiB 的预算会在 Linux 上把内存压力上限重新压回来。
- 修复 Service Worker 更新会自行刷新页面的问题。更新改为弹出非阻塞提示条，激活前先
  经过安全刷新守卫，正在编辑的内容因此得以保留，提示会在内容保存后重新出现。
- 修复 Discover 两处在没有任何持久化确认的情况下就提示"已保存"的问题，现在改为
  "已提交"。
- 修复源码扫描类契约测试因换行符而非因它们要保护的语句顺序而失败的问题。

### 安全

- Team 的运行入口、首次设置、升级、管理、通知、后台任务、API 和公开导出在当前个人版
  中全部默认关闭并失败封闭。已有 Team 源码和历史数据只做归档，不会偷偷改归个人或删除。
- MCP / Skill 权限绑定真实登录账号、明确 scope、当前授权版本、到期时间和服务端撤销。
  安装客户端不会自动得到管理员权限，也不能绕过申请所有权。
- 外部招生数据源分别保留速率、超时、缓存、重试、User-Agent 与来源规则；返回结构变化、
  模糊身份、私网目标或缺少证据时都会失败封闭。
- 源码分发审计只忽略 Git 明确标记为删除的文件，其他任何读不到的源码仍会阻止发布；
  公开导出也继续拒绝私有路径和运行时数据。
- Strict-Transport-Security 只下发一次。应用侧继续发送，使得使用其他反向代理、云负载
  均衡或直连 TLS 的部署同样受到保护；随附的 Nginx 模板会先隐藏上游那份，再添加自己的。
- 为推荐人接口新增越权隔离测试，覆盖跨账号读取与非成员访问团队接口两种情形。
- 确认邮件分类能抵抗提示词注入：一封指示模型返回固定分类并原样输出系统提示词的邮件
  被判定为无关内容，且没有泄漏任何提示词。

### 性能与规模

- 最终类生产门禁服务了 300 个登录用户、300 个常驻 SSE 和同一地址 300 个健康
  WebSocket，读取和登录均没有容量重试；随后验证 300 次持久写入和重启后 300 次读回。
- 最终耐久门禁完成 5,372/5,372 次持久自动保存、52 次后台自动保存、180 个后台任务、
  100 个 SSE、100 个健康 WebSocket，以及 11,500 次连接读取；连接读取 P95 为 248.5ms。
- 同一次门禁完成 70/70 个大型工作区流且没有重启，重启后 100/100 次读回成功；健康、
  读取、登录和混合请求 P95 分别为 217.9、592.2、500.6、1,196.2ms，所有预留均归零。
- 大库校验改为分批读取紧凑元数据，不再一次性装下所有 ID，也不再每条发一个原生查询。
  真实 2 万行加密库始终一次只解码一个载荷，并保持事件循环和内存有界。
- 大工作区响应、加解密、压缩、登录、普通请求、健康流量和后台任务现在都有明确、按体积
  区分的通道，一项昂贵工作不会悄悄吃掉另一项必须保留的容量。
- 构建入口预算确保未登录页面和条件功能样式不会进入已登录工作区首屏；最终生产构建
  包含 1,231 个模块，并通过当前 JavaScript/CSS 体积门禁。

### 验证

- 最终整机交互验证通过 9 个文件 / 144 个测试；动效和工作区契约通过 7 个文件 /
  34 个测试。
- TypeScript、生产构建、发布契约、源码安全审计、定向 lint、Node 语法与差异检查均在
  最终资格状态通过。
- 国际化结构与键覆盖通过 12 种语言 × 18 个命名空间；最终公共 UI/错误提示审计为零问题。
- 公开导出、仅个人运行边界、更新包可复现性、回滚验证、Microsoft SQL Server 门禁，
  以及 amd64/arm64 容器冒烟仍是发布前必须通过的硬门槛。

## [0.1.0-beta.8] - 2026-07-31

### 新增

- 新增统一的可恢复懒加载模块边界，覆盖路由、工作区页面、弹窗、个人与 Team
  资料编辑器、富文本编辑器和语言命名空间，并提供有界重试、共享请求和旧资源恢复。
- 新增本地化的应用级恢复页面；模块或渲染持续失败时不再只显示白页，用户仍可明确
  重新加载，并看到已保存工作不会丢失的提示。
- 新增 288 张真实产品截图，覆盖 6 个登录后工作流、12 种语言、亮色/暗色主题，
  以及真实桌面和手机布局。
- 新增跨进程原子上传保险库迁移锁，支持等待存活所有者、清理已终止所有者并按令牌
  释放锁。
- 新增经过完整性校验的文件级 Release 差异包。只有基线完全匹配的安装才下载新增/
  变化文件和删除清单，再在本地重建完整包；差异包不可信或体积没有更小时自动回退
  完整包。

### 变更

- 未登录首页不再使用手工搭建的产品替身，改为展示真实 Checklist、
  Correspondence、Funding、Timeline、Discover 和 Profile 界面；新截图完成解码前
  会继续保留上一张成功图片。
- 公开版中英文 README 使用同一组真实产品截图，新增响应式亮色/暗色界面图集；
  6 个页面现在默认全部可见，同时仍可整体收起。
- `react-simple-code-editor` 继续保持懒加载，并在一个带类型的运行时边界统一处理
  Vite 的 CommonJS/ESM 包装。
- 在不改变语言、主题、桌面/手机布局和像素尺寸的前提下，以高视觉质量 WebP 参数
  重新编码 288 张 DPR 2 截图：截图集由 66.16 MB 降至 22.81 MB；即使加入差异
  更新运行时代码，本地对照构建的完整更新包仍缩小 25.6%（72.46 MB → 53.94 MB）。

### 修复

- 修复旧动态资源或编辑器包装对象进入 React 后，打开申请、新建申请或新增资料片段
  时出现白页的问题。
- 修复多个 watch API 进程共用上传保险库迁移日志并导致开发服务器不稳定的问题。
- 修复未登录工作流条目因观察器竞态持续透明，以及产品场景切换时闪现破损图片的问题。
- 修复未提供 `matchMedia` 的类浏览器环境打开未登录产品图集时崩溃的问题，现会安全保留
  桌面截图。

## [0.1.0-beta.7] - 2026-07-29

### 新增

- 新增持久化的用户邮件与系统邮件 outbox，保留稳定投递标识、定时发送、重试元数据、
  过期占用恢复、启动处理和附件保险库引用。
- 新增持久邮箱同步任务、通知/摘要恢复和加密浏览器推送日志，已经接受的后台工作在
  重启后不会丢失。
- 新增统一的安全富文本协议，覆盖 Markdown/HTML 编辑、GFM 渲染、源码高亮、
  格式化、上下文补全、清理、不可变已发送快照和 multipart SMTP 投递。
- 新增由服务端授权的 AI 邮件附件规划，可从 Profile、Checklist、任务和安全通信
  文件中选择，但不会向浏览器暴露保险库句柄或文件内容。
- 新增必须使用 AI 的现有申请增强流程，包含有界上下文、链接审计、定向搜索、
  公网抓取、提示词注入隔离、独立证据核验和可审核、幂等的新增项。
- 新增 120 个专业研究画像、OpenAlex Topic 校验、带机构核验的 Crossref 回退，
  以及按学科启用的 Europe PMC 线索；可保存事实仍只接受学校官方页面。
- 新增 225 个内容互不重复、保留来源与许可的学校 PNG 标识，直接覆盖 145 个内置
  Discover 学校适配器中的 119 个。
- 新增个人/Team 智能表格、渐进分页、批量选择、行内状态编辑、保留状态的看板/
  表格切换，以及响应式 Team 学生肖像钻取。
- 新增申请自动保存、按时间戳处理的离线协调、Team 批量邀请校验、收件人管理、
  Checklist 指针排序和共享通信身份显示。

### 变更

- Team 权限改为所有者管理的角色默认值加稀疏个人覆盖，能力与使用限额仍由服务端
  权威执行。
- 立即发送和定时发送会先于 SMTP 持久化，共用同一通信记录，并在重试时复用精确的
  HTML/纯文本快照。
- 申请切换、看板/表格、Settings 渐进加载、大型表格、Checklist 披露和浮层定位
  改用有界、可中断的动效，同时减少布局读取与 React 提交。
- 未登录产品故事现在在电脑与手机上真实复现 Applications、Discover、Profile、
  Dossier、Checklist、Correspondence、Funding、Timeline 和 Inspector 工作流。
- 来源授权、AI 附件选择、发件附件编辑、临时收件人和缺少附件确认统一到一条邮件
  草稿所有权路径。

### 修复

- 修复旧 API 失败重新打开已恢复熔断器、重复健康 WebSocket 握手、传输故障触发
  启动回退扇出，以及开发重启被常驻 WebSocket/SSE 长连接拖住的问题。
- 修复申请选择过冲、看板/表格切换抖动、分页追加扫描整张表、移动导航交接遮挡、
  选择器边距漂移、紧凑操作被裁切及多种窄屏翻译换行问题。
- 修复邮箱文件夹发现和单邮件失败隔离、精确通信对方头像、多收件人分类、导入邮件
  安全边界，以及危险通信误入 AI 上下文的问题。
- 修复空个人工作区的数据导出、自动保存冲突恢复、Team 并发编辑决策和遗留手动
  草稿路径。

### 安全

- 新增 DNS 固定的出站 HTTPS、严格生产 Host 策略、安全请求目标日志、密码校验
  工作量上限和私有邮件目标白名单。
- 新增精确文件签名、图像及 ZIP/OOXML 资源上限、活动内容拒绝、来信威胁分析，
  未知入站格式只允许下载。
- 新增源码密钥/运行时数据审计并记录跨界面威胁模型；高危 npm 依赖审计保持为零。

### 性能

- 集中管理浏览器请求合并和全局 API 熔断，保留按尝试隔离的实时连接，并让服务端
  周期任务既在启动时执行，又始终不重叠。
- 提前预热渐进表格批次和看板/表格目标，复用大型常驻视图，并把高频动效限制在
  局部 ref、CSS 变量和合成器 transform。

## [0.1.0-beta.6] - 2026-07-28

### 新增

- 新增 `npm run verify:published-update -- --from <version> --tag <tag>`
  公开版本验收命令：使用生产更新器下载真实 Release，然后重放包校验、安装、
  活动包记录和回滚安全检查。
- 新增随 Release 交付、逐项匹配 lockfile 完整性的完整生产依赖归档，并在发布
  门禁中使用空缓存执行真实离线 `npm ci`。

### 变更

- Release 传输会分别限制等待响应头的时间，以及流式下载中每一次连续无进度的
  时间；只要仍持续收到字节，即使官方源较慢也不会被误判失败。
- 镜像探测仍延迟到官方源不可达或停止产生数据之后。失败的官方连接会被取消，
  残缺文件会先删除，再从 gzip 签名正确的固定 HTTPS 镜像中选择最快来源。
- 生产依赖改为直接从标准 manifest 和 lockfile 自动生成，不再维护第二份手写
  运行时白名单；发布与旧版恢复会在有界时间内依次使用原始源、npmjs、
  npmmirror、Yarn 及适用的 GitHub 镜像，并始终校验 lockfile 完整性。
- 已编译进 `dist/` 的前端库归为构建依赖；今后新增的每一个服务端依赖都会自动
  进入更新包。

### 修复

- 修复官方源成功返回两个 gzip 探针字节后，正式包正文停止传输却仍被当作健康来源
  的问题；现在会及时回退，而不是等完整下载的长超时结束后仍未尝试镜像。
- 修复另一进程在 Windows 卷上新增或删除备份、但目录时间戳没有前进时，备份列表
  仍复用旧缓存的问题；并发读取仍只共享一次目录扫描和逐文件元数据缓存。
- 修复依赖安装可以无限等待的问题：每个来源会写入持久心跳，受到无进展、单次和
  总时限约束，卡住的进程树会被终止，并继续保留已验证回滚。

### 新增

- 新增持久化服务端更新任务：管理员关闭浏览器后，下载、校验、备份、依赖安装、重启
  和首次启动交接仍会继续执行。
- 新增隐私脱敏且容量受限的更新日志，持久记录阶段、进度、失败原因、npm 输出和首次
  启动回滚诊断；管理员重新打开页面或服务重启后仍可查看。

### 变更

- Release 更新包只保留服务端运行所需的依赖图。已经打包进 `dist/` 的前端依赖（包括
  外部 SheetJS 归档）不再在服务器原地更新时下载。
- 更新依赖安装会复用持久 npm 缓存并优先使用缓存，同时继续执行精确 lockfile 安装、
  运行时预检、首次启动确认和自动回滚。
- public/private 发布工作流会把 Draft 与最终 Release 元数据固定到规范版本标签和
  已打标签的公开提交。
- 公开 `main` 容器发布会等待同一提交已经成功的 CI，不再重复整套树门禁；MSSQL
  发布门禁与最终发布共用一个 runner 和一次依赖安装，避免门禁通过后再次排队。

### 修复

- 修复更新状态、日志和 Release 检查错误占用“每小时 80 次”的认证上传限额。旧版每
  650ms 一次的状态轮询会在不到一分钟后持续收到 `429`，使已经成功的更新看起来卡住。
- 修复服务器能访问 GitHub 或镜像、但无法访问仅前端使用的包 CDN 时，Beta 到 Beta
  更新会在下载后的 `npm ci` 阶段失败。
- 修复旧服务停止后只留下泛化错误的问题；依赖安装、助手进程启动、运行时预检和首次
  启动回滚现在都会留下可操作的持久诊断。
- 修复锚定弹层延迟恢复焦点的任务在组件卸载后仍存活、随后访问已销毁浏览器/测试
  `window` 的问题。

### 新增

- 新增隐私保护的注册、登录、找回密码和首次配置挑战：使用不透明的一次性记录、
  有界尝试与过期时间，提供网络、账户、域名、子网和全局持久限流，可选 Turnstile，
  并让注册响应保持枚举安全。
- 新增细粒度 Team 学生/教师能力，覆盖申请、Discover、邀请、分享、转移以及可选的
  活跃/累计额度；分配关系和额度全部由服务端权威校验。
- 新增仅限个人申请的离线快照/队列 v3、最长 72 小时授权租约、离线启动页，以及
  回放时的服务端所有权、冲突和版本校验。
- 新增持久化后台系统日志分页、搜索、排序、流式 CSV/JSON 导出，以及“永久保存”或
  有限天数的保留策略。
- 新增可交互的未登录产品工作区、对应的 Pro 容量演示、文档流内项目页脚和无障碍
  支持弹窗。
- 新增账户级自定义申请状态、实验室/项目档案链接、应用目标深链提示，以及全局截断
  文本展开和复制交互。

### 变更

- 新密码使用更严格的上下文/黑名单校验、可选 HIBP k-anonymity 查询和 Argon2id
  存储；旧 bcrypt 登录成功后会原地迁移。
- Team Discover 只使用当前机构由管理员管理的密钥；教师审计仅限负责的学生；
  public/private 构建共用同一套 Team 授权模型。
- 离线模式会立即缩减已经挂载的内存工作区；生产 Service Worker 仅缓存壳层和明确
  静态资源，绝不缓存 API、带 Cookie、private/no-store 或任意 JSON 响应。
- 自动更新现在完整展示解析、探测、下载、校验、准备、重启和可重试失败状态；使用
  有界 HTTPS 镜像传输时，官方 GitHub 元数据和 SHA-256 仍是唯一权威。
- 校徽解析优先选择紧凑官方标识并复用内容寻址资源；Team 看板、肖像库、申请切换、
  锚定弹层和移动端控制均获得范围受控、兼容减少动态效果的交互优化。
- 发布校验统一使用严格的源码/公开树门禁、强制 TypeScript build-mode、独立公开版
  安装、确定性更新包、Compose 校验，以及真实 amd64/arm64 生产冒烟。

### 修复

- 修复申请转移无限检查、过期跳转意图重放、截断值不可访问、卡片/列表切换回弹、
  Team 瀑布流空洞、弹层重定位/退出闪烁、固定本地端口冷启动误判，以及多个 PWA
  Passkey/Push 失败状态。
- 修复公开首次配置与 Team 测试/运行时边界：生产环境进入一次性管理员初始化，
  开发和测试继续保留确定性夹具。

### 安全

- CAPTCHA 答案、邮件/配置验证码、重置链接和 SMTP 验证码不再进入浏览器可读 JWT
  或邮件审计正文；安全与 SMTP 审计元数据保持隐私安全。
- 密码、角色和禁用状态变更通过 `authVersion` 撤销会话；固定 JWT 算法、签发者和
  受众，拒绝不安全跨站 Fetch Metadata 写请求，并在生产环境对宽泛代理信任失败关闭。
- 离线回放从当前服务端记录恢复服务端所有的能力字段；浏览器本地完整性数据绝不被
  当作授权证明，也不声称能对抗已被攻陷的设备。

### 性能

- 高频选择、平移、缩放和布局动效改用范围受控的 ref、CSS 变量、批量几何读取、
  有界合成层过渡和临时图层清理，避免 React 渲染抖动。
- 更新邮件解析、表格处理和开发工具依赖；依赖审计已清零。

## [0.1.0-beta.5] - 2026-07-26

### 新增

- 公共 Beta 现已提供完整 Team 协作：机构角色、邀请、范围受控工作空间、申请协作、
  审计历史和 Team AI 访问。
- 首次 SMTP 配置会从已填写的发件邮箱向通知收件箱发送六码验证码，验证通过后才能创建
  首位管理员。

### 变更

- 公开导出现在保留 Team 运行时代码、语言包，以及与私有源码一致的服务端权限校验。
- 收件人分组 CSV 导入升级为带模板、拖放、文件选择和预览的聚焦动画流程。

### 修复

- 安全密钥复制会保留完整内容；验证邮件会正确渲染 HTML，而不再显示源码。
- Discover 跳转 AI 密钥配置前会保存当前调研草稿；设备通知部署异常时会给出有界错误，
  不再永久转圈。
- 优化后台窄屏导航、触控选中反馈、成对字段尺寸、辅助文字层级与公开界面翻译。

## [0.1.0-beta.4] - 2026-07-24

### 新增

- 首次 `/admin` 初始化页面支持亮色/暗色主题切换和语言切换（12 种语言），与
  管理员登录页已有的控制项保持一致。
- 三种平台 Docker 部署脚本全覆盖（Windows CMD/PowerShell、Linux/Ubuntu、
  宝塔面板三种方式：图形界面、SSH 终端、Docker Compose）。每种平台提供
  独立、带颜色标注、可直接复制使用的脚本及管理命令。

### 变更

- 部署文档（`DEPLOYMENT.md` 和 `DEPLOYMENT.zh-CN.md`）现在以三种部署方案
  开篇，并包含宝塔面板分步配置表格。

### 修复

- `bootstrapSecrets.js` 中使用 `fileURLToPath` 替代 `__filename`，实现 ESM 兼容。
- JWT 长度检查现在仅在生产模式下生效。
- 安全相关 i18n 键和 `CONFIRMATION_REQUIRED` 错误信息已覆盖全部 12 种语言。

### 性能

- Docker 镜像从 1.14 GB 缩减至 826 MB（多阶段 Alpine 构建，esbuild 打包
  入口，移除开发依赖和非 Linux 二进制文件）。
- 新增 NJU 镜像站（`ghcr.nju.edu.cn`）文档，供 GitHub Container Registry
  访问较慢地区的用户使用。

## [0.1.0-beta.3] - 2026-07-24

### 新增

- Docker 一键部署：只需 `docker run --env DOMAIN=` 即可启动，JWT 和加密密钥
  首次启动自动生成并持久化到 `storage/bootstrap-secrets.json`。
- URL 自动推导：从单个 `DOMAIN` 变量自动推导 `BASE_URL`、`CORS_ORIGIN`、
  `ALLOWED_HOSTS`。
- Admin 初始化流程新增安全密钥步骤：引导流程中展示自动生成的密钥，支持
  一键重新生成、复制按钮和破坏性操作的二次确认。

### 变更

- 安装和部署指南缩减至原来的 1/4，聚焦 Docker 一条命令上线，类似
  Vaultwarden 风格。
- 最小化 `.env.example`：仅需 `DOMAIN`，其余均为可选覆盖项。
- `latest` Docker 标签现已与 `beta` 一同发布，指向同一个最新 Beta 版本。

## [0.1.0-beta.2] - 2026-07-23

**预发布版本 — Beta。** 在首个稳定版发布前，数据库结构、已存数据和升级路径
仍可能发生不向后兼容的变化。安装或更新前，请先创建完整工作空间备份，在应用
停止后复制整个 `storage/` 目录或 Docker 卷；如果使用外部数据库，还应同时创建
该数据库的匹配快照。

### 新增

- 新增服务器首次使用时的一次性 `/admin` 配置流程。管理员可以选择 SQLite、
  MySQL/MariaDB、PostgreSQL 或 Microsoft SQL Server，测试连接，并且只能在
  合适的空目标上完成首次配置。
- 新增 Admin 中后续测试数据库连接和迁移工作空间的控制项；外部数据库密码会
  加密保存。
- 新增公开 Docker 镜像 `ghcr.io/zhoujasper/phd-atlas`，包括滚动更新的 `beta`
  通道和固定版本的预发布标签。Beta 发布工作流同时构建 `linux/amd64` 与
  `linux/arm64`，并且有意不发布 `latest` 标签。
- 新增 Admin 自动更新检查，更新源固定为公开的
  `zhoujasper/phd-atlas` GitHub Releases。发现兼容的新版本后，管理员可以先
  查看 Release 页面，再一键安装。
- 新增手动上传 Release 更新包的备用入口，适用于离线或网络受限的服务器。
  Docker、systemd 和 WinSW 部署共用同一套已验证安装包和受控重启流程。
- 新增 Docker 持久更新重放：已验证并激活的 Release 包保存在
  `storage/active-update/`；即使容器由较旧基础镜像重建，也会重新应用该包，
  全程不需要访问 Docker socket。

### 变更

- Release 更新现在会先创建完整工作空间备份，保留 `.env`、已选数据库、上传
  文件和已有备份，并且只替换受管理的运行时代码。
- Docker 与原生启动器现在会协调更新锁，在安装后重启应用 worker，启动时确认
  候选运行时；若启动验证失败，则恢复上一套运行时和之前的激活包指针。
- 公开 Release 自动化现在会构建名称确定的
  `phd-atlas-update-<version>-release.tar.gz` 及配套 `.sha256` 文件，测试安装
  与回滚流程，并将验证后的资产附加到 GitHub 预发布版本。
- 多架构容器冒烟测试现在会在 `linux/amd64` 与 `linux/arm64` 之间清理
  Docker 本地的多架构清单缓存，确保两个变体都被独立拉取和运行验证后，才会
  提升任何公开标签。标签提升后的 GHCR 匿名摘要检查也会进行有界重试，避免
  注册表传播延迟造成发布误报失败。

### 安全

- Release 检查只接受规范的 SemVer 标签，以及固定公开仓库中唯一且符合预期的
  安装包与校验文件组合。
- 更新包内容指纹现在采用与区域设置无关的归档路径排序，因此同一个确定性安装包
  可以在 Windows 与 Linux 上得到一致的验证结果。
- Release 下载仅允许 HTTPS，并限制重定向次数、时间和大小；激活前会校验
  SHA-256、包清单、声明版本、文件集合、解压路径、条目类型和解压上限。
- 中断或未完成的更新会安全失败；诊断标记和日志会保留，回滚不完整时则阻止
  部分更新的运行时继续启动。

### 文档

- 新增详细的英文和简体中文安装指南，覆盖 Docker、首次 Admin 配置、四类
  数据库选择、日常使用、备份、更新和故障排查。
- 扩充部署指南，覆盖 Docker Compose、Ubuntu 与通用 Linux、CentOS
  Stream/RHEL 兼容系统、Windows WinSW、反向代理、TLS/私有 CA、持久存储、
  数据库迁移和 Beta 回滚。
- 更新公开版 README，补充受支持数据库矩阵、公开 GHCR 镜像与标签策略、手动
  与自动 Release 更新路径，以及现有 `0.1.0-beta.1` 安装的一次性升级说明。

## [0.1.0-beta.1] - 2026-07-20

### 新增

- 发布首个可自托管、隐私优先、单工作空间的公开 Beta。
- 新增申请增删改查、仪表盘分析、列表/Kanban 导航、完整档案、材料清单、
  推荐信、奖学金、任务、费用、进度和统一时间线。
- 新增项目与导师发现、个人资料复用、通信记录、限定范围的 IMAP/SMTP、
  附件、受控分享链接、多格式导出、日历与通知。
- 新增完整工作空间备份、后台管理、集成密钥加密、PWA/离线支持、响应式布局、
  无障碍偏好、亮暗主题与 12 种语言。
- 发布首个 GitHub Release 更新包及 SHA-256 校验文件。

[0.1.0]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0
[0.1.0-beta.8]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.8
[0.1.0-beta.7]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.7
[0.1.0-beta.6]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.6
[0.1.0-beta.5]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.5
[0.1.0-beta.4]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.4
[0.1.0-beta.3]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.3
[0.1.0-beta.2]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.2
[0.1.0-beta.1]: https://github.com/zhoujasper/phd-atlas/releases/tag/v0.1.0-beta.1
