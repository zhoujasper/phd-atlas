/**
 * Both private source and the public Beta ship the supported Team feature set.
 * The public exporter preserves this value so entitlement and route behaviour
 * remain identical across the two GitHub repositories.
 */
export const PUBLIC_EDITION = false
